on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
    

jobs:
  metrics_generate:
    runs-on: ubuntu-latest
    steps:
      - name: Metrics embed
        uses: lowlighter/metrics@v3.34
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
